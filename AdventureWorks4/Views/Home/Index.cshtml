@{
	ViewData["Title"] = "Home Page";
}

@Html.Partial("Permissions")

<div class="text-center">
	<div class="row">
		@* @foreach (var resort in ViewBag.ResortList)
		{
			<div class="card m-2" style="width: 18rem;">
				<img src="../@resort.Photo" class="card-img-top" style="width: 270px; height: 180px">
				<div class="card-body">
					<h5 class="card-title">@resort.Name</h5>
					<p class="card-text">@resort.Description</p>
					<p class="card-text">$@resort.Price per night</p>
					<a class="btn btn-primary">Book this!</a>
				</div>
			</div>
		} *@
	</div>
</div>
